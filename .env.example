# Claude API Key (optional - for AI-powered recommendations)
# Get your API key from https://console.anthropic.com/
VITE_CLAUDE_API_KEY=your_api_key_here

# Enable AI recommendations (requires API key above)
# Set to 'true' to use Claude AI, 'false' to use local weighted random
# If AI fails (rate limit, no credits), it automatically falls back to local
VITE_USE_AI=false

# Feedback email - where user feedback will be sent
VITE_FEEDBACK_EMAIL=your-email@example.com
