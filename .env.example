# ===========================================
# CLIENT-SIDE ENVIRONMENT VARIABLES (VITE_*)
# These are bundled into the app and visible to users
# ===========================================

# Enable AI recommendations
# Set to 'true' to use Claude AI via serverless function
# Set to 'false' to use local weighted random only
# If AI fails (rate limit, no credits), it automatically falls back to local
VITE_USE_AI=false

# Feedback email - where user feedback will be sent via mailto:
VITE_FEEDBACK_EMAIL=your-email@example.com


# ===========================================
# SERVER-SIDE ENVIRONMENT VARIABLES
# Set these in Netlify dashboard (Site settings > Environment variables)
# These are NEVER exposed to the browser
# ===========================================

# Claude API Key (set in Netlify, NOT here)
# Get your API key from https://console.anthropic.com/
# CLAUDE_API_KEY=sk-ant-your-key-here
